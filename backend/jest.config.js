module.exports = {
  testEnvironment: 'node',
  testMatch: [
    "**/src/tests/**/*.test.js",
  ],
  // Optional: if you have setup files that run before tests
  // setupFilesAfterEnv: ['./jest.setup.js'],
};
